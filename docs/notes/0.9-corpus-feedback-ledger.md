# 0.9 Corpus Feedback Ledger

Working ADR-037 ledger for ranked browser-vs-PowerPoint audit rows. Raw render
artifacts stay under `reports/visual/...`; this file records only durable
classification decisions.

| Date | Row | Outcome | Evidence | Decision |
| --- | --- | --- | --- | --- |
| 2026-05-01 | `tests/svg/text-tspan-02-b.svg` | `measurement-fixed` | Bundled W3C PNG footer is `$Revision: 1.10 $`, while the local SVG source is `$Revision: 1.11 $`. | Skip the stale W3C PNG oracle and use a live browser reference for this row. |
| 2026-05-01 | `tests/svg/text-intro-02-b.svg` | `measurement-fixed` | Bundled W3C PNG footer is `$Revision: 1.2 $`, while the local SVG source is `$Revision: 1.10 $`; live browser rendering matches the current source footer and materially reduces the mismatch. | Skip the stale W3C PNG oracle and use a live browser reference for this row. |
| 2026-05-01 | `tests/svg/text-intro-09-b.svg` | `measurement-fixed` | Bundled W3C PNG footer is `$Revision: 1.3 $`, while the local SVG source is `$Revision: 1.7 $`; the stale oracle accounts for the active high score. | Skip the stale W3C PNG oracle and use a live browser reference for this row. |
| 2026-05-01 | `tests/svg/filters-overview-03-b.svg` | `measurement-fixed` | `tests/png/filters-overview-03-b.png` is byte-identical to `filters-overview-02-b.png` and shows `$Revision: 1.1 $`, while the local SVG source is `$Revision: 1.2 $`. | Skip the stale W3C PNG oracle and use a live browser reference for this row. |
| 2026-05-01 | `tests/svg/filters-conv-05-f.svg` | `measurement-fixed` | Bundled W3C PNG footer is `$Revision: 1.1 $`, while the local SVG source is `$Revision: 1.2 $`; the PNG also lacks the current DRAFT watermark that live browser rendering shows. | Skip the stale W3C PNG oracle and use a live browser reference for this row. |
| 2026-05-01 | `tests/svg/text-text-07-t.svg` | `measurement-fixed` | Bundled W3C PNG footer is `$Revision: 1.4 $`, while the local SVG source is `$Revision: 1.6 $`. | Skip the stale W3C PNG oracle and use a live browser reference for this row. |
| 2026-05-01 | `tests/svg/text-text-09-t.svg` | `measurement-fixed` | Bundled W3C PNG footer is `$Revision: 1.5 $`, while the local SVG source is `$Revision: 1.7 $`. | Skip the stale W3C PNG oracle and use a live browser reference for this row. |
| 2026-05-01 | Filter group bitmap fallback rows | `measurement-fixed` | Structure comparison zips source leaves to PPTX leaves even when one filter fallback bitmap intentionally replaces several source shapes, producing misleading count and bbox priority. | Suppress only count/bbox score penalties for `group_filter_fallback_rendered` bitmap rows; keep visual mismatch and bitmap counts active. |
| 2026-05-01 | `tests/visual/fixtures/resvg/transform_torture.svg` | `accepted-limitation` | Chromium treats the fixture's SVG 1.1-incompatible `<g>` child inside `<clipPath>` as an empty clip, while the converter intentionally resolves nested clip geometry for resvg stress coverage. | Keep converter semantics and mark the audit row as an ADR-037 accepted limitation so it does not block the next ranked target. |
| 2026-05-01 | `tests/svg/filters-overview-02-b.svg` | `fixed` | The raster input-surface path resolved object-bounding-box filter bounds, then reused those absolute bounds in a descriptor still marked `objectBoundingBox`, causing BackgroundImage/BackgroundAlpha assets to render blank. Source-derived user-space StrokePaint also painted only the original geometry instead of a filter-region paint swatch. | Treat internally resolved filter bounds as user-space bounds for temporary source/background SVGs, and derive full paint surfaces for solid/user-space paint inputs while keeping object-bounding-box gradient paint geometry-bound. |
| 2026-05-01 | `tests/svg/filters-overview-02-b.svg`, `tests/svg/filters-overview-03-b.svg` | `deferred` | After the bounded fixes, the rows still differ on exact SourceGraphic/SourceAlpha blur footprint and broader filter input-source semantics. `filters-overview-03-b.svg` also lacks a stable oracle: the bundled PNG is stale and Chromium does not render SVG 1.1 BackgroundImage/BackgroundAlpha inputs. | Suppress these residual rows as ADR-037 deferred work and leave the broader filter source-surface parity pass outside the current 0.9 closure slice. |
| 2026-05-01 | `tests/svg/text-dom-01-f.svg` | `accepted-limitation` | This W3C DOM conformance row depends on `onload` JavaScript mutating the SVG tree before comparison, while the converter intentionally ignores `<script>` and event-handler attributes for security. | Keep static no-script conversion semantics and mark the row as an ADR-037 accepted limitation so scripted DOM execution does not block static fidelity work. |
| 2026-05-01 | `tests/svg/text-intro-02-b.svg`, `tests/svg/text-intro-09-b.svg`, `tests/svg/text-tspan-02-b.svg` | `deferred` | Live browser references and PPTX output agree on visible text content, bidi/rotation semantics, and placement, but SSIM remains below threshold from native editable text antialiasing, font fallback, and renderer differences between Chromium and the PPTX renderer. | Suppress these residual rows as ADR-037 deferred text-renderer parity work; closing them needs broader font/raster parity or semantic text-oracle scoring rather than a narrow converter patch. |
| 2026-05-01 | `tests/svg/filters-conv-05-f.svg` | `accepted-limitation` | With the live browser reference, the row passes the browser threshold; remaining score is from three expected bitmap fallbacks for `feConvolveMatrix edgeMode`, which OOXML cannot express natively. | Keep bitmap fallback behavior and mark the row as an ADR-037 accepted limitation so required filter rasterization does not block static fidelity work. |
| 2026-05-01 | `tests/svg/text-text-07-t.svg`, `tests/svg/text-text-09-t.svg` | `deferred` | Live browser references and PPTX output agree on per-glyph x/y/rotate placement, and both rows pass the browser threshold; remaining low score is native editable text rasterization noise between Chromium and the PPTX renderer. | Suppress these residual rows as ADR-037 deferred text-renderer parity work; closing them needs broader font/raster parity or semantic text-oracle scoring rather than a narrow converter patch. |

## Mimic Ideas: W3C “Essential Google Slides Template for Internet”

- Deep-scan source: `/Users/ynse/Downloads/0.6 The Essential Google Slides Template for Internet.pptx`  
  Artifacts: `reports/visual/w3c_mimic_inventory/mimic_inventory_thorough.json` and `.md` (2026-05-01 snapshot).
- Priority patterns to mimic (from repeated usage, not one-off cases):
  1. `slide / ellipse / gradFill / path=circle / fillToRect=True / shadow=True` — 66 shapes.  
     This is the core "fake top-down radial" motif.
  2. `layout / custGeom / gradFill / path=circle / fillToRect=True / odd-rotation` — 16 shapes (`-135°`) plus `slide` variants (6 shapes including `-136.93°`, `85.27°`, `93.68°`, `-179.98°`, and `6.10°`).  
     This is the rotated-gradient mimic family (most likely geometric rotation tricks).
  3. `overlap-like` and `mesh-like` blocks both at 66; many blocks are paired duplicate/triplicate overlays with shared bounds and mixed `solidFill`/`gradFill`.
  4. `trapezoid / gradFill` with 2–3 stops (9 shapes) and `custGeom / gradFill / path=circle` variants are the second-order palette effects.
- Top-risk concrete samples to treat as canonical regression anchors:
  - `ppt/slides/slide27.xml`: shape IDs `1064`, `1076`, `1067`, `1065`, `1069`
  - `ppt/slides/slide65.xml#2459`
  - `ppt/slideLayouts/slideLayout12.xml`: shape IDs `129`, `143`, `150`, `160`, `167`, `170`, `219`
- Suggested implementation order:
  1. Implement reusable radial fake background/mask recipe for the 66-slide ellipse-circle-fillToRect family.
  2. Add odd-rotation-aware radial/custGeom transform handling for the `-135°`/`~85°` families.
  3. Recreate the small shape-overlap mesh pattern (`solidFill + gradFill` at same bounds) as a grouped mimic primitive with deterministic stack order.
